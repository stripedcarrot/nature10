<p id="notice"><%= notice %></p>

<p>
  <b>Text:</b>
  <%= @comment.text %>
</p>
<p> User:</p>
<%= @comment.user_id %>
<p>
  <b>Date:</b>
  <%= @comment.date %>
</p>

<p>
  <b>Parent:</b>
  <%= @comment.parent_id %>
</p>
<%= link_to 'Original Comment', photo_path(@comment.photo.id, anchor: 'comment'), class: "btn btn-turquoise btn-large"   %>

<%= link_to 'Edit', edit_comment_path(@comment) %> |
<%= link_to 'Back', comments_path %>
